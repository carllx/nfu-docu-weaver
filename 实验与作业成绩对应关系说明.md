# AI 助手指南：将评分标准文档转换为结构化打分表 (Excel)

本指南旨在指导 IDE 智能助手如何解析 `.doc/docx` 格式的评分标准，并生成符合教务要求的 `x课程x班打分表.xlsx` 结构。

## 第一阶段：文档解析逻辑 (Parsing Logic)

当接收到一个评分标准文档时，AI 应按以下顺序提取信息：

### 1. 提取元数据 (Metadata Extraction)

从文档页眉或开头提取基本信息：

- **课程名称**：搜索“课程名称”关键字后的文本。
    
- **适用对象**：提取学年、学期、专业、班级信息。
    
- **考核方式**：区分是“演示类”、“实操类”还是“理论类”。
    

### 2. 构建多级评价体系 (Hierarchical Structure)

- **一级指标**：识别文档中的大标题（如“一、选题切合度”、“二、视觉表现”）。记录其对应的总分值。
    
- **二级指标**：识别大标题下的子项（如“1. 主题诠释”、“2. 界面美学”）。
    
- **采分点描述**：抓取二级指标下的具体要求文字。这是生成“评价标准”列或单元格批注的核心。
    
- **分值区间**：抓取描述中的分值建议（如“9-10分”、“6-9分”），用于设置 Excel 的数据验证或提示。
    

## 第二阶段：Excel 结构化映射 (Spreadsheet Mapping)

生成的 Excel 结构必须遵循以下布局：

### 1. 表头区域 (Row 1-4)

- **标题行**：`[学年][学期] [课程名称] 期末考查阅卷打分表`。
    
- **基本信息行**：包含 选课课号、教师姓名、学分、学时、上课地点等占位符。
    

### 2. 字段映射区域 (Row 5 & 6)

这是转换的核心，需采用 **双行表头** 模式：

- **固定字段列**：`序号`、`学号`、`姓名`、`专业`、`班级`。
    
- **动态指标列 (关键)**：
    
    - **第五行 (一级指标)**：合并单元格，显示“一级指标名称（总分）”。
        
    - **第六行 (二级指标)**：拆分单元格，显示“序号. 二级指标名称（该项分值）：具体描述”。
        
- **汇总字段列**：`总分`（公式列）、`评语`（文本列）。
    

## 第三阶段：生成示例 (Template Output)

针对提供的《交互产品综合创作》案例，转换后的 Excel 结构应如下所示：

|        |        |        |                   |                  |         |           |         |
| ------ | ------ | ------ | ----------------- | ---------------- | ------- | --------- | ------- |
| **序号** | **学号** | **姓名** | **一、选题与策划 (20分)** | **二、设计规范 (30分)** | **...** | **总分**    | **评语**  |
|        |        |        | **1.主题诠释(10分)**   | **2.界面美学(15分)**  | ...     |           |         |
| 1      | [占位]   | [占位]   | (填写 0-10)         | (填写 0-15)        | ...     | =SUM(...) | [针对性建议] |

## 第四阶段：给 AI 的 Prompts 指令模板

当用户要求你转换文档时，请执行以下思考流程：

1. **扫描标题**：确定有多少个“一、二、三...”。
    
2. **拆解子项**：每个“一”下面有多少个“1, 2...”。如果文档没写二级指标，则将一级指标作为打分项。
    
3. **提取权重**：确保所有二级指标分值之和等于总分（通常为100）。
    
4. **生成表头**：
    
    - 将二级指标的文字描述（如“视觉风格高度统一...”）精简后放入 Excel 第六行的表头内。
        
    - 设置 Excel 单元格公式：`总分 = 所有二级指标列之和`。
        

## 五、 通用性原则 (Universality)

- **学科中立**：不预设它是艺术类还是理工类，只根据文档中的“分值”和“文本描述”进行对齐。
    
- **容错处理**：如果文档只有一级指标，则 Excel 只生成一级指标列；如果文档没有分值，则平均分配 100 分。
    
- **反馈引导**：在“评语”列预留提示——“请根据二级指标的得分高低，针对性给出优缺点评价”。