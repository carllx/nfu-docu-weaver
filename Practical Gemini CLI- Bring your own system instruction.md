# 智能评分助手：技术架构与操作全景 (SmartGrading Assistant)

本方案旨在为教师提供一个在 Mac OS 本地运行的、基于“审美授权”逻辑的辅助评分工具。

## 1. 核心技术底座：Mac 本地轻量化引擎

- **硬件加速**：利用 `PyTorch + MPS` 调用 Apple Silicon 的神经网络引擎。
    
- **硬件灵活性**：模型权重及原始素材可存放在外置 SSD。
    
- **模型选型**：`CLIP ViT-B/32` + `DINOv2 small` + `bge-small-zh`。
    
- **版本冻结原则**：模型与 Embedding 版本在一个教学周期（如一学期）内禁止升级，确保评分“标尺”的一致性。
    

## 2. 素材预处理与特征失效探测

为了解决大文件加载及算法局限，引入以下逻辑：

1. **离线转码**：生成 WebP 轻量副本及视频关键帧长图。
    
2. **特征失效探测 (Fail-safe)**：
    
    - **判定规则**：若图片像素熵（Pixel Entropy）低于阈值（如全黑、纯白、极简线条）或语义距离分布过度集中。
        
    - **系统行为**：主动弹出提示，判定该批次为“特征不可判别”，关闭自动分桶，引导进入“人工全量复核模式”。
        

## 3. 多模态素材的“双通道”处理逻辑

### A. 课程类型预设权重模板

不同类型的作业，其评分权重应自动调整：

| 课程类型 | 语义通道 (CLIP) | 结构通道 (DINO) | 质量信号 (熵/密度) |

| 3D 建模临摹 | 0.4 | 0.5 | 0.1 |

| 交互设计/创意 | 0.7 | 0.2 | 0.1 |

| 操作演示/录屏 | 0.3 | 0.4 | 0.3 |

## 4. 核心交互：人机耦合的“锚点分桶”机制

### 第一阶段：初始化分桶

教师手动挑选 A/B/C/D 等级的标杆，建立初始质心。

### 第二阶段：动态收敛与“软刹车”约束

1. **自动分桶**：基于余弦相似度进行初步归类。
    
2. **锚点漂移监控**：
    
    - 系统记录每次迭代中新旧锚点的平均距离变化。
        
    - **报警机制**：若连续多次迭代导致质心大幅漂移（暗示教师标准出现疲劳偏离），系统提示：“当前标准已显著偏离初始设定，是否复核前序已评样本？”
        

## 5. 教学公信力与质量安全网 (核心补充)

### A. 评分版本冻结 (Grading Snapshot)

为应对教学申诉，每次提交成绩前系统自动生成 `grading_snapshot.json`：

- **内容**：包含当时的所有锚点 ID、各桶质心向量、权重参数及时间戳。
    
- **意义**：确保每一份作业的分数都有据可查，可追溯其判分时的逻辑状态。
    

### B. 算法可解释性出口 (Explainable UI)

虽不对学生公开，但为教师提供“解释锦囊”：

- **展示项**：当前作业与所属桶锚点的平均相似度、主要的偏离维度（如“结构差异过大”）。
    
- **用途**：帮助教师在学生质疑成绩时，能有理有据地指出其作品在形体或逻辑上的具体差距。
    

## 6. 工程落地与安全防护

- **离群值雷达**：强制人工介入置信度低（距离各桶均较远）的样本。
    
- **插件回写**：采用“语义锚定”防止 DOM 变更失效，填分后由教师手动点击“确认回写”，守住教务风险底线。
    

## 7. 紧迫周期下的 MVP 策略

1. **Day 1**：跑通 Python MVP 脚本，验证模型在特定课程素材上的相似度区分力。
    
2. **Day 2 上午**：构建简单的“分桶视图”UI，实现拖拽改分。
    
3. **Day 2 下午**：生成带有“评分版本信息”的 Excel 导出，完成第一次实战测试。
    

技术结论：

SmartGrading Assistant 并非试图“自动判断好坏”，而是通过“显性锚点 + 局部一致性计算”，在 M1 Mac 的有限算力下，帮助教师在复杂、多模态的作业中保持评分标准的稳定、高效与可解释性。