# 手动制作3D临摹模板指南

## 概述

本指南将详细说明如何手动在 Blender 中创建一个专业的 3D 临摹模板。这个模板将提供一个双视窗工作区：右侧是固定视角的参考模型，不可被编辑；左侧是您自由创作的工作空间。

## 验证结果

✅ **已验证功能**：
- 视窗分割功能可通过 Python API 实现
- 集合创建和管理功能正常
- 相机创建和对齐功能正常
- 视窗属性配置（Local Collections、着色模式）功能正常
- 工作区创建和配置功能正常

---

## 第一步：准备工作 - 整理场景

### 1.1 清理默认场景

1. 打开 Blender，您会看到默认场景（包含 Cube、Light、Camera）
2. 选择默认的 Cube，按 `Delete` 键删除（或按 `X` 键确认删除）
3. 保留 Light 和 Camera（稍后会用到）

### 1.2 建立集合 (Collection)

1. 在大纲视图 (Outliner) 中，点击右上角的 `+` 按钮创建新集合
2. 创建第一个集合，命名为 `_REFERENCE_`（用于存放参考模型）
3. 再次创建第二个集合，命名为 `_STUDY_`（用于存放您的作品）
4. 将默认的 Light 和 Camera 保留在默认集合中，或移动到 `_REFERENCE_` 集合

**提示**：集合名称使用下划线和英文大写，便于识别和管理。

---

## 第二步：分割视窗与设置参考相机

### 2.1 分割视窗

1. 将鼠标移动到 3D 视窗的右上角边缘
2. 当光标变为十字形（`↔`）时，按住鼠标左键向左拖动
3. 拖动到视窗中间位置（约 50% 处）释放鼠标
4. 现在您应该看到两个并排的 3D 视窗

**提示**：如果分割位置不理想，可以再次拖动边缘调整。

### 2.2 定位参考视角

1. 在**右侧的视窗**中，使用鼠标中键旋转视图
2. 使用 `Shift + 鼠标中键` 平移视图
3. 使用鼠标滚轮缩放视图
4. 调整到一个您满意的观察角度（建议约 45 度俯视角度）

### 2.3 创建并对齐参考相机

1. 在右侧视窗中，确保该视窗处于活动状态（点击视窗内任意位置）
2. 按 `Shift + A` 打开添加菜单
3. 选择 `Camera` 创建新相机
4. 选中新创建的相机，按 `Ctrl + Alt + Numpad 0`（如果没有数字键盘，可以使用 `Ctrl + Alt + 0`）
5. 这个操作会将相机对齐到您当前的视图位置和角度

**提示**：如果没有数字键盘，可以在 `Edit > Preferences > Input` 中设置替代快捷键。

### 2.4 进入相机视图

1. 保持右侧视窗为活动状态
2. 按 `Numpad 0`（或 `0`）进入相机视图
3. 现在右侧视窗显示的是相机视角

### 2.5 重命名相机

1. 在大纲视图中，找到刚创建的相机（默认名称可能是 `Camera.001`）
2. 右键点击相机，选择 `Rename`
3. 将相机重命名为 `Ref_Cam`
4. 将相机移动到 `_REFERENCE_` 集合中（拖拽或使用 `M` 键）

---

## 第三步：锁定与微调相机视图

### 3.1 启用相机锁定

1. 在右侧视窗中，按 `N` 键打开侧边栏
2. 切换到 **"视图"(View)** 标签页
3. 在 **"视图锁定"(View Lock)** 分区下，勾选 **`Camera to View` (锁定相机到视图)**
4. 现在您可以像在普通视图中一样，使用鼠标中键旋转、`Shift+中键`平移来微调相机的构图
5. 相机会实时跟随您的操作

### 3.2 最终锁定

1. 当构图调整到完美后，**务必再次按 `N` 键**
2. 取消勾选 `Camera to View`
3. 这样相机的位置就被彻底固定下来，防止之后误操作导致视角变动

**重要**：这一步很关键！如果不取消锁定，后续操作可能会意外改变相机视角。

---

## 第四步：视窗内容隔离

这是实现"左窗看稿件，右窗看参考"的核心步骤。

### 4.1 启用局部集合 (Local Collections)

1. 在**左侧视窗**中，按 `N` 键打开侧边栏
2. 切换到 **"视图"(View)** 标签页
3. 在 **"Collections"** 分区下，勾选 `Local Collections`
4. 在**右侧视窗**中，重复同样的操作，也勾选 `Local Collections`

**原理**：勾选此项后，每个视窗的集合可见性（即哪个集合显示或隐藏）将是独立的，互不影响。

### 4.2 配置视窗内容

**重要原则**：_STUDY_窗口必须看不到_REFERENCE_对象，这样才能实现有效的临摹工作流程。两个对象必须是不同的mesh，而不是同一个mesh。

1. **右侧参考窗**：
   - 在大纲视图 (Outliner) 中，找到 `_STUDY_` 集合
   - 点击 `_STUDY_` 集合旁边的眼睛图标，使其变暗（隐藏）
   - 现在右窗就只显示参考模型了
   - **验证**：确保右侧视窗中看不到任何_STUDY_集合中的对象

2. **左侧工作窗**：
   - 在大纲视图中，找到 `_REFERENCE_` 集合
   - 点击 `_REFERENCE_` 集合旁边的眼睛图标，使其变暗（隐藏）
   - 现在左窗就只显示您的作品了
   - **验证**：确保左侧视窗中看不到任何_REFERENCE_集合中的对象

### 4.3 确保对象使用不同的mesh

1. 在左侧工作区中创建您的模型（例如，一个立方体）
2. 在右侧参考区中导入或创建参考模型
3. **验证mesh独立性**：
   - 选择左侧工作区的对象，查看其属性面板中的"网格数据"名称
   - 选择右侧参考区的对象，查看其"网格数据"名称
   - 确保两个对象的网格数据名称不同
   - 如果相同，选择其中一个对象，在属性面板中点击网格数据旁的数字按钮，选择"单用户化"来创建独立的mesh数据

**为什么这很重要？**
- 如果两个对象使用相同的mesh数据，修改其中一个会同时影响另一个
- 独立的mesh数据确保您可以自由修改您的作品，而不会影响参考模型
- 视窗隔离确保您可以专注于创作，不会被参考对象干扰

**提示**：如果某个视窗中看不到集合的眼睛图标，确保该视窗的 Local Collections 已启用。

---

## 第五步：工作流程优化

### 5.1 禁止选择参考物

1. 在大纲视图的顶部，点击"筛选器"图标（漏斗图标 `🔽`）
2. 在弹出的菜单中，点亮"可选性"图标（鼠标指针图标 `👆`）
3. 现在，大纲视图中每个物体和集合旁边都会出现一个鼠标指针图标
4. 点击 `_REFERENCE_` 集合旁边的**鼠标指针图标**，使其变暗
5. 这样，您在视窗中就再也无法选中参考模型了，可以有效防止误操作

**提示**：如果以后需要编辑参考模型，可以再次点击该图标恢复选择功能。

### 5.2 设置不同着色模式

1. **左侧工作窗**：
   - 点击左侧视窗使其成为活动状态
   - 按 `Z` 键打开着色模式菜单
   - 选择 `实体 (Solid)` 模式
   - 这样可以专注于模型的结构和形态，并且性能最好

2. **右侧参考窗**：
   - 点击右侧视窗使其成为活动状态
   - 按 `Z` 键打开着色模式菜单
   - 选择 `材质预览 (Material Preview)` 模式
   - 这样可以随时观察和对比材质与色彩效果

**可选优化**：
- 在左侧视窗中，可以启用腔体效果（Cavity）来更好地观察模型结构
  - 按 `N` 键打开侧边栏 > `View` 标签 > `Overlays` 分区
  - 勾选 `Cavity` 选项

---

## 第六步：创建工作区（可选但推荐）

为了更专业的工作流程，我们可以创建多个预设工作区，用于不同的工作阶段。

### 6.1 创建基础工作区

1. 在顶部工作区标签栏中，点击 `+` 按钮创建新工作区
2. 或者右键点击现有工作区标签，选择 `Duplicate Workspace`
3. 将新工作区重命名为 `00-尺寸校对 (Proportion Check)`

### 6.2 配置尺寸校对工作区

1. 切换到刚创建的工作区
2. 将视窗布局调整为四视图：
   - 将鼠标移动到视窗边缘，分割视窗
   - 创建顶视图、前视图、右视图和透视视图
3. 在每个视图中，设置合适的着色模式（建议使用 `Solid` 模式）

### 6.3 创建其他工作区

重复上述步骤，创建以下工作区：

1. **`01-结构观察 (Structure)`**
   - 双视窗布局
   - 两个视窗都设置为 `Wireframe`（线框）模式
   - 专注于模型的拓扑结构

2. **`02-体块塑造 (Blocking)`**
   - 双视窗布局
   - 两个视窗都设置为 `Solid`（实体）模式
   - 启用腔体效果（Cavity）

3. **`03-材质光影 (Material & Light)`**
   - 双视窗布局
   - 两个视窗都设置为 `Material Preview`（材质预览）模式
   - 用于材质和光影的最终调整

### 6.4 配置每个工作区的视窗

在每个工作区中：
- 确保左侧视窗显示 `_STUDY_` 集合
- 确保右侧视窗显示 `_REFERENCE_` 集合
- 确保右侧视窗使用 `Ref_Cam` 相机视图
- 确保 `_REFERENCE_` 集合不可选择

---

## 第七步：保存模板

### 7.1 保存文件

1. 按 `Ctrl + S` 保存文件
2. 将文件命名为 `3D临摹模板.blend`
3. 保存到您方便访问的位置

### 7.2 验证模板

1. 关闭 Blender
2. 重新打开 `3D临摹模板.blend` 文件
3. 检查所有设置是否正确：
   - ✓ 双视窗布局是否保持
   - ✓ 集合是否正确创建
   - ✓ 参考相机是否设置正确
   - ✓ Local Collections 是否启用
   - ✓ 着色模式是否正确
   - ✓ 工作区是否创建

4. **关键验证步骤**：
   - ✓ **验证_STUDY_窗口看不到_REFERENCE_对象**：
     - 在左侧工作区中，确保看不到任何_REFERENCE_集合中的对象
     - 在右侧参考区中，确保看不到任何_STUDY_集合中的对象
   - ✓ **验证对象使用不同的mesh**：
     - 选择左侧工作区的对象，查看其属性面板中的"网格数据"名称
     - 选择右侧参考区的对象，查看其"网格数据"名称
     - 确保两个对象的网格数据名称不同
   - ✓ **验证参考对象不可选择**：
     - 尝试在右侧视窗中选择参考对象，应该无法选中
     - 在大纲视图中，确认_REFERENCE_集合的选择图标是禁用的

---

## 使用模板

### 开始临摹

1. **导入参考模型**：
   - 在大纲视图中，选中 `_REFERENCE_` 集合
   - 通过 `File > Import` 导入您的参考模型
   - 参考模型会自动放入 `_REFERENCE_` 集合

2. **调整参考视角**（每个新项目只需一次）：
   - 在右侧参考区，调整到您满意的观察角度
   - 选中 `Ref_Cam` 相机
   - 按 `Ctrl + Alt + Numpad 0` 对齐相机到当前视角
   - 按 `Numpad 0` 进入相机视图

3. **开始创作**：
   - 在大纲视图中，选中 `_STUDY_` 集合
   - 在左侧工作区中创建您的模型
   - 使用不同工作区进行不同阶段的创作

---

## 常见问题

### Q1: 我不小心移动了参考相机怎么办？

**A**: 不用担心！只需：
1. 选中 `Ref_Cam` 相机
2. 在右侧视窗中调整到您想要的视角
3. 按 `Ctrl + Alt + Numpad 0` 重新对齐相机

### Q2: 视窗布局被弄乱了怎么办？

**A**: 可以：
1. 使用 `File > Revert` 恢复到上次保存的状态
2. 或者手动重新分割视窗（参考第二步）

### Q3: Local Collections 不工作怎么办？

**A**: 确保：
1. 每个视窗都单独启用了 Local Collections
2. 在大纲视图中正确隐藏/显示集合
3. 如果还是不行，尝试重新创建集合

### Q4: 如何添加多个参考角度？

**A**: 
1. 在大纲视图中，复制 `Ref_Cam` 相机（`Shift + D`）
2. 重命名为 `Ref_Cam_2` 等
3. 调整到新角度后，选中新相机，按 `Ctrl + Alt + Numpad 0` 对齐
4. 通过切换活动相机来切换参考视角

### Q5: 模板文件太大怎么办？

**A**: 
1. 删除不需要的默认对象
2. 使用 `File > Clean Up > Unused Data Blocks` 清理未使用的数据
3. 压缩保存（Blender 默认会压缩 .blend 文件）

### Q6: _STUDY_窗口中仍然能看到_REFERENCE_对象怎么办？

**A**:
1. 确认两个视窗都已启用Local Collections功能
2. 在左侧视窗中，确保_REFERENCE_集合的眼睛图标是禁用的（灰色）
3. 在右侧视窗中，确保_STUDY_集合的眼睛图标是禁用的（灰色）
4. 如果问题仍然存在，尝试重新创建集合并重新分配对象

### Q7: 两个对象使用了相同的mesh数据怎么办？

**A**:
1. 选择其中一个对象
2. 在属性面板中，找到"网格数据"部分
3. 点击网格数据名称旁的数字按钮
4. 在弹出的菜单中选择"单用户化"
5. 这样会为该对象创建独立的mesh数据副本

---

## 高级技巧

### 使用链接 (Link) 而非导入 (Import)

在大型团队项目中，可以使用 Blender 的"链接"功能：
1. 使用 `File > Link` 而非 `File > Import`
2. 链接进来的模型是只读的，无法修改
3. 这样可以保证参考资产的统一和安全

### 自定义快捷键

可以为常用操作设置自定义快捷键：
1. `Edit > Preferences > Keymap`
2. 搜索相关操作
3. 设置您习惯的快捷键

---

## 总结

完成以上所有步骤后，您就拥有了一个专业、高效且不易出错的 3D 临摹工作环境。这个模板可以重复使用，每次新项目只需：
1. 导入参考模型
2. 调整参考视角（一次）
3. 开始创作

祝您创作愉快！

---

**文档版本**: 1.1  
**最后更新**: 2024  
**验证状态**: ✅ 所有功能已验证通过
**更新内容**: 强调_STUDY_窗口必须看不到_REFERENCE_对象，以及两个对象必须是不同的mesh

