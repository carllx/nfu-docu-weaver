# 🌍 从本地到 World - Netlify 把你的作品集挂上互联网的三种方式

各位艺术家们，如果你已经在本地做好了网页，现在是时候把它展示给全世界了。

Netlify 是我们发布网站的“云端展厅”。这里有三种把画作（代码）挂上墙（服务器）的方法，难度依次递增。

今天，我们主要掌握 **第一种**，它是最直观、最符合直觉的方式。

## 🟢 Level 1：手动拖拽部署 (Manual Drop)

**⭐️ 推荐新手使用 | 难度：⭐ | 核心逻辑：像网盘一样简单**

这是最适合大家的入门方式。你不需要写任何代码，只需要会用鼠标。

### 就像把画框搬进画廊

想象一下，你整理好了一个文件夹，里面装满了你的画作。你只需要把它直接“扔”进 Netlify 的大厅，它就会自动帮你挂好。

### 📝 操作步骤

1. **准备账号**：
    
    - 登录 [Netlify 官网](https://www.netlify.com/ "null")。
        
    - 如果是第一次，点击 Sign up 注册（推荐用 Email 或 GitHub）。
        
2. **找到上传区**：
    
    - 登录后进入 Dashboard（仪表盘），点击 **"Sites"**（站点）标签页。
        
    - 屏幕下方通常会有一个虚线框区域，写着 _Drag and drop your site output folder here_。
        
    - **⚠️ 没看到虚线框？** 请点击右上角的 **"Add new site"** 按钮，选择 **"Deploy manually"**，上传界面就会出现。
        
3. **检查文件夹 (关键一步！)**：
    
    - 确保你的文件夹里有一个主文件叫 `index.html`。
        
    - **注意**：必须全是**小写字母**！不能叫 `Index.html` 或 `home.html`，否则服务器看不懂。
        
    - 确保 `index.html` 就在文件夹的**第一层**，不要把它藏在子文件夹里。
        
4. **拖拽上传 (Drag & Drop)**：
    
    - **动作**：鼠标左键按住整个文件夹，直接拖进网页里的那个虚线框，松手。
        
5. **等待发布**：
    
    - 你会看到一个上传进度条。几秒钟后，屏幕变绿，状态变成 **"Published"**。
        
    - Netlify 会随机给你分配一个像 `https://happy-artist-123456.netlify.app` 这样的网址。
        
6. **查看作品**：
    
    - 点击那个绿色的网址，你的网站上线了！发给朋友们炫耀一下吧。
        

> 缺点提醒：
> 
> 这种方法很方便，但每次你修改了代码（比如改了一个错别字），你都必须重新拖拽整个文件夹再上传一次。

## 🟡 Level 2：Git 版本控制部署 (Git Integration)

**⭐️ 推荐进阶使用 | 难度：⭐⭐⭐ | 核心逻辑：自动化流水线**

当你开始接触 Git 和 GitHub 后，这是最主流、最专业的发布方式。目前大家只需了解有这个功能，等后续代码能力提升后使用。

### 就像拥有了魔法传送门

你不再需要手动搬运画作。你在本地修好画，点一下“保存并推送”，画廊里的画就会自动变成新的。这叫 **CI/CD (持续集成/持续部署)**。

### ⚙️ 原理简介

1. **关联仓库**：在 Netlify 上选择 "Import from Git"，连接你的 GitHub 账号。
    
2. **选择项目**：选中你存放代码的那个仓库 (Repository)。
    
3. **自动构建**：
    
    - 设置好后，每当你把代码 `push` 到 GitHub，Netlify 会立刻监测到。
        
    - 它会自动下载新代码 -> 打包 -> 更新网站。
        
    - 全程不需要你登录 Netlify 官网操作。
        

## 🔴 Level 3：命令行部署 (Netlify CLI)

**⭐️ 极客/开发者模式 | 难度：⭐⭐⭐⭐ | 核心逻辑：黑客式操作**

这是给习惯使用终端（Terminal）的高级玩家准备的。它通过代码指令来控制服务器，和我们之前学的“本地预览”用的是同一个工具箱。

### 就像在控制台远程操控

你坐在工作室里，通过敲击键盘指令，远程控制画廊的灯光和展位。

### ⌨️ 简要流程

1. 安装与登录：
    
    打开终端输入以下命令：
    
    ```
    npm install -g netlify-cli
    # Mac 用户如果报错 Permission denied，请在前面加 sudo，如：sudo npm install ...
    ```
    
    然后登录：
    
    ```
    netlify login
    ```
    
2. 一行命令发布：
    
    在你的项目文件夹里，输入：
    
    ```
    netlify deploy
    ```
    
    它会问你几个问题（比如“你要发布哪个文件夹？”），一路回车确认即可。
    
3. 正式上线：
    
    上面的命令通常是发布一个“草稿预览版”。如果要正式替换线上版本，需要加一个参数：
    
    ```
    netlify deploy --prod
    ```
    

## 💡 总结与建议 (Summary)

|   |   |   |   |
|---|---|---|---|
|**方法**|**适合阶段**|**优点**|**缺点**|
|**Level 1: 拖拽法**|**今天！现在！**|0代码，直观，几秒钟搞定。|修改内容麻烦，每次都要重新拖。|
|**Level 2: Git法**|下个月 / 团队协作|全自动，不用管它，不仅存了代码还发了网。|需要先学会 Git 和 GitHub。|
|**Level 3: CLI法**|未来的全栈大师|可以在写代码的过程中直接发布，不用开浏览器。|需要记命令，看起来比较枯燥。|

👨‍🏫 老师的作业：

今天请大家全部使用 Level 1 (拖拽法)，把你们刚才做的静态页面发布上线，并把生成的 URL 链接提交上来！